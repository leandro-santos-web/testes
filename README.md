# No terminal, na pasta do projeto:
git init
git add .
git commit -m "Forno & Batata - Pizzaria"

# Crie um repositório no GitHub e depois:
git remote add origin https://github.com/SEU-USUARIO/forno-batata.git
git branch -M main
git push -u origin main
